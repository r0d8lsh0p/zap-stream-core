[build]
builder = "DOCKERFILE"
dockerfilePath = "crates/zap-stream-external/Dockerfile"

[deploy]
startCommand = "/app/bin/zap-stream-external"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
